# Copy to supabase/functions/.env for local Edge Functions (never commit).
# Loaded automatically on `supabase start` when this file exists as .env
#
# Generate after BGG approves your application:
# https://boardgamegeek.com/applications → Tokens
BGG_TOKEN=
